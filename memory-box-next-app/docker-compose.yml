version: '3.8'

services:
  memory-box:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    volumes:
      - ./out:/usr/share/nginx/html:ro
    restart: unless-stopped
